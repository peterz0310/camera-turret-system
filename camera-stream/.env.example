ESP32_CAM_URL=http://192.168.4.62/stream

# Rotate incoming frames before serving: 0, 90, 180, or 270
STREAM_ROTATE=0

# If rotation creates portrait output, center-crop back to landscape
STREAM_FORCE_LANDSCAPE=false

# Optional output resize (set both >0 to enable)
STREAM_OUTPUT_WIDTH=0
STREAM_OUTPUT_HEIGHT=0
